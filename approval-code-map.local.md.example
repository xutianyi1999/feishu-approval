# 审批显示名称 ↔ approval_code（本地映射示例）

将本文件**复制**为 **`approval-code-map.local.md`**（与 `SKILL.md` 同级）后填写真实行。  
**`approval-code-map.local.md` 已被 `.gitignore` 忽略**，`git pull` / 提交上游更新时不会冲突。

查表顺序与全文约定见仓库根目录 **`approval-code-map.md`**。

## 映射表

| 审批显示名称（与飞书内一致） | approval_code |
|------------------------------|---------------|
| _示例：请假（请删除）_ | `00000000-0000-0000-0000-000000000001` |
