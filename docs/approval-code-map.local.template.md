# 审批显示名称 ↔ approval_code（本地映射）

将本文件**复制**为仓库根目录的 **`approval-code-map.local.md`** 后填写真实行（或运行 **`feishu-approval-tool util init`** 在根目录生成同名文件）。**`approval-code-map.local.md` 已在 `.gitignore`**，不会进入 Git。

约定见 **`docs/AI.md` §8**。

## 映射表

| 审批显示名称（与飞书内一致） | approval_code |
|------------------------------|---------------|
| _示例：请假（请删除）_ | `00000000-0000-0000-0000-000000000001` |
